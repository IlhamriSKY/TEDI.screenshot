{
  "name": "tedi-screenshot",
  "version": "0.5.5",
  "private": true,
  "type": "module",
  "description": "Build tooling for the TEDI Screenshot extension. The shipped artifact is the bundled extension.js (built in CI into the release .zip, not committed); source lives in src/.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "node --check extension.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
